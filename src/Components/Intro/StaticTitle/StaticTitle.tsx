import * as React from 'react'
import styles from './StaticTitle.module.scss'

const StaticTitle = (state: any) =>
    <div className={styles.staticTitleContainer}>hi</div>

export default StaticTitle